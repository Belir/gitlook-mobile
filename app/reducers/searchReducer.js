const initialState = {
  data: {
    searchPhrase: '',
  },
  status: {},
  error: {},
};

const searchReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default searchReducer;